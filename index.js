/**
 * The default export contains all of our stylelint rules.
 */
module.exports = {
  /**
   * Extend base configs.
   */
  extends: ['./base', './warning'],

  /**
   * The following rules will override the configuration from "extends".
   * https://stylelint.io/user-guide/configuration/#extends
   */
  rules: {
    // Add rules here.
  },
};
